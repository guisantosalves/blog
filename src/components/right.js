import React from "react";
import { Button, Container, Row, Col } from "react-bootstrap";

const Right = (props) => {
  return (
    <Col sm={9} >
      <h1>RIGHT</h1>
    </Col>
  );
};

export default Right;
